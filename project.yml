# HomeHub — XcodeGen project specification
#
# Generate the Xcode project:
#   brew install xcodegen   (if needed)
#   xcodegen generate
#
# Then open HomeHub.xcodeproj in Xcode.
#
# ── External binary dependency ────────────────────────────────────────────────
# llama.xcframework must be placed as a sibling of the repo root, e.g.:
#   ~/Developer/
#     llama.xcframework/       ← here
#     ios-home-hub/            ← repo root (this project)
#
# The paths below use $(SRCROOT)/.. which expands to that parent directory.
# If your layout differs, override LLAMA_FRAMEWORK_PATH in a local xcconfig.
# ─────────────────────────────────────────────────────────────────────────────

name: HomeHub

options:
  bundleIdPrefix: cz.keksiczek
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.4"
  createIntermediateGroups: true
  generateEmptyDirectories: false

packages:
  # mlx-swift uses semver; `from: 0.10.0` pins to the latest 0.x compatible
  # release and is safe to use as-is. The committed Package.resolved locks the
  # exact revision so builds are fully reproducible.
  MLX:
    url: https://github.com/ml-explore/mlx-swift
    from: 0.21.0
  MLXLM:
    url: https://github.com/ml-explore/mlx-swift-lm
    branch: main
  # Explicit direct dependency on swift-transformers.
  # HubIntegration.swift imports Hub and Tokenizers unconditionally.
  # Listing the package here (rather than relying on mlx-swift-lm's
  # transitive pull) locks the version and avoids TensorUtils conflicts.
  # from: 0.1.14 is the minimum version that works with WhisperKit 0.11.0.
  SwiftTransformers:
    url: https://github.com/huggingface/swift-transformers
    from: 0.1.14
  # WhisperKit ≥ 0.11.0: first release that resolves TensorUtils conflicts
  # with swift-transformers 0.1.x. VoiceService.swift gates all WhisperKit
  # usage behind #if canImport(WhisperKit) so the app still compiles if
  # this package is removed; keeping it here enables the on-device STT path.
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit
    from: 0.11.0

  # swift-transformers: the `Hub` and `Tokenizers` library products used by
  # HubIntegration.swift were introduced after the last semver tag (0.1.8) and
  # are only available on the main branch. Once HuggingFace cuts a release that
  # includes them, switch to `from: <version>`.
  # Risk: same as MLXLM above. Mitigation: Package.resolved pins the revision.
  swiftTransformers:
    url: https://github.com/huggingface/swift-transformers
    branch: main

  # WhisperKit is pinned to an exact version — fully stable.
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit
    exactVersion: 0.9.3

settings:
  base:
    SWIFT_VERSION: "5.10"
    GENERATE_INFOPLIST_FILE: YES
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    ENABLE_USER_SCRIPT_SANDBOXING: YES

targets:
  HomeHub:
    type: application
    platform: iOS
    entitlements:
      path: HomeHub/HomeHub.entitlements
      properties:
        com.apple.security.application-groups:
          - group.cz.keksiczek.homehub.shared
    sources:
      - path: HomeHub
    dependencies:
      # ── Binary framework (llama.cpp) ─────────────────────────────────────────
      # Path is relative to the repo root: ../llama.xcframework means the
      # framework lives one directory above this repo (sibling of the checkout).
      - framework: ../llama.xcframework
        embed: true
        codeSign: true
      # System frameworks required by llama.cpp's Metal backend.
      # Without explicit linking, the linker emits auto-link-failure warnings
      # for Accelerate (BLAS/vDSP), Metal compute, and CoreAudio.
      - sdk: Accelerate.framework
      - sdk: Metal.framework
      - sdk: MetalKit.framework
      - sdk: MetalPerformanceShaders.framework
      - sdk: CoreAudio.framework
      # ── Swift Package dependencies ────────────────────────────────────────────
      - package: MLX
        product: MLX
      - package: MLX
        product: MLXNN
      - package: MLXLM
        product: MLXLLM
      - package: MLXLM
        product: MLXLMCommon
      - package: swiftTransformers
        product: Hub
      - package: swiftTransformers
        product: Tokenizers
      - package: WhisperKit
        product: WhisperKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cz.keksiczek.homehub
        # Resolve llama.xcframework relative to the source root (project dir).
        # $(SRCROOT) == directory containing HomeHub.xcodeproj, so .. is the
        # parent where llama.xcframework lives.
        FRAMEWORK_SEARCH_PATHS: "$(inherited) $(SRCROOT)/.."
        HEADER_SEARCH_PATHS: "$(inherited) $(SRCROOT)/../llama.xcframework/ios-arm64/Headers"
        OTHER_LDFLAGS: "$(inherited) -lc++"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: >-
          UIInterfaceOrientationPortrait
          UIInterfaceOrientationLandscapeLeft
          UIInterfaceOrientationLandscapeRight
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: >-
          UIInterfaceOrientationPortrait
          UIInterfaceOrientationPortraitUpsideDown
          UIInterfaceOrientationLandscapeLeft
          UIInterfaceOrientationLandscapeRight
        NSMicrophoneUsageDescription: "Potřebujeme mikrofon pro možnost hlasové konverzace offline."
        NSSpeechRecognitionUsageDescription: "Nativní rozpoznávání hovoru pro diktování přes Apple infrastrukturu beze slov mimo zařízení."
        # App uses only standard HTTPS/TLS for model downloads and no custom
        # cryptography — declare exemption so App Store review doesn't flag
        # the build as missing an export-compliance answer. If this changes
        # (e.g. adding non-exempt crypto), remove this key and answer the
        # encryption questionnaire in App Store Connect instead.
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: "$(inherited) HOMEHUB_REAL_RUNTIME"
        SWIFT_OBJC_BRIDGING_HEADER: "HomeHub/Runtime/Bridge/HomeHub-Bridging-Header.h"
        GCC_PREPROCESSOR_DEFINITIONS: "$(inherited) HOMEHUB_REAL_RUNTIME=1"
        #
        # ─── SwiftData persistence (optional) ────────────────────────────────────
        # Uncomment to replace the JSON FileStore with SwiftData:
        # SWIFT_ACTIVE_COMPILATION_CONDITIONS: "$(inherited) HOMEHUB_SWIFTDATA"
        #
        # ─────────────────────────────────────────────────────────────────────────
    dependencies:
      - package: MLX
        product: MLX
      - package: MLX
        product: MLXNN
      - package: MLXLM
        product: MLXLLM
      - package: MLXLM
        product: MLXLMCommon
      # Hub + Tokenizers for HubIntegration.swift (direct imports, not via Transformers).
      - package: SwiftTransformers
        product: Hub
      - package: SwiftTransformers
        product: Tokenizers
      # WhisperKit for VoiceService.swift on-device STT.
      - package: WhisperKit
        product: WhisperKit
    preCompileScripts: []
    postCompileScripts: []

  HomeHubTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: HomeHubTests
    dependencies:
      - target: HomeHub
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cz.keksiczek.homehub.tests
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/HomeHub.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/HomeHub"
        BUNDLE_LOADER: "$(TEST_HOST)"

  HomeHubWidget:
    type: app-extension
    platform: iOS
    entitlements:
      path: HomeHubWidget/HomeHubWidget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.cz.keksiczek.homehub.shared
    sources:
      - path: HomeHubWidget
    dependencies:
      - target: HomeHub
        embed: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cz.keksiczek.homehub.widget
        INFOPLIST_KEY_CFBundleDisplayName: HomeHub
        INFOPLIST_KEY_NSExtension_NSExtensionPointIdentifier: com.apple.widgetkit-extension
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: "$(inherited)"
